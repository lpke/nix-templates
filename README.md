# nix-templates

Nix flake templates I copy into projects to set up local development environments with direnv.

**Usage:** Copy a template's files into a project, then run `direnv allow`.

Don't forget to include the contents of `gitignore_sample` in your project's `.gitignore`.

---

Most flexible `node` template: [`node_with_bin`](./node_with_bin)
